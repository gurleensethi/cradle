name: Go
description: Template for a basic Go project

input:
  - name: GoVersion
    default: "1.25"
    required: true
    help: Specify the Go version for the project.

  - name: ModuleName
    type: string
    help: The module name for the Go project (e.g., github.com/username/projectname).
    required: true

files:
    go.mod: |
        module {{.ModuleName}}

        go {{.GoVersion}}

    main.go: |
        package main

        import "fmt"

        func main() {
            fmt.Println("Hello {{ .ProjectName }}!")
        }

    README.md: |
        # {{.ProjectName}}

        This is a basic Go project template.

    .gitignore: |
        # Binaries for the current project
        *.exe
        *.dll
        *.so
        *.dylib
        *.o

        # Compiled Go packages
        *.a

        # Test binary, generated by `go test -c`
        *.test

        # Go module cache
        /pkg/mod/

        # Dependency directories (if not using Go modules)
        vendor/

        # Log files
        *.log

        # Editor/IDE specific files
        .idea/
        .vscode/
        *.swp
        *.bak
        *~

        # Operating System generated files
        .DS_Store
        Thumbs.db
